{%- capture urls -%}
{%- endcapture -%}

{%- render 'delay-load',
urls: urls,
snippets: '',
on_delay_load: on_delay_load,
load_apps: true,
delay: 1000,
delay_load: true
-%}
{%- render 'delayed-scripts' -%}
